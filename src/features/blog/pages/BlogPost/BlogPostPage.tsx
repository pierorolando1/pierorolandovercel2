import { MDXRemote, MDXRemoteSerializeResult } from "next-mdx-remote"
import { PostType } from "../../types/post"

export default ({
    post,
    source
}: { post: PostType, source: MDXRemoteSerializeResult }) => {
    return (
        <>

        </>
    )
}
